@model Stat_reports.Models.ReportModel

@{
    ViewData["Title"] = "Создать отчет";
}

<h1>Создать отчет</h1>

<form asp-action="Create" method="post" enctype="multipart/form-data">
    <div class="form-group">
        <label asp-for="BranchId" class="control-label">Филиал</label>
        <select asp-for="BranchId" class="form-control">
            <option value="">Выберите филиал</option>
            @foreach (var branch in ViewBag.Branches)
            {
                <option value="@branch.Id">@branch.Name</option>
            }
        </select>
        <span asp-validation-for="BranchId" class="text-danger"></span>
    </div>

    <div class="form-group">
        <label asp-for="TemplateId" class="control-label">Шаблон отчета</label>
        <select asp-for="TemplateId" class="form-control">
            <option value="">Выберите шаблон</option>
            @foreach (var template in ViewBag.ReportTemplates)
            {
                <option value="@template.Id">@template.Name</option>
            }
        </select>
        <span asp-validation-for="TemplateId" class="text-danger"></span>
    </div>

    <div class="form-group">
        <label class="control-label">Загрузить отчет</label>
        <input type="file" name="file" class="form-control" />
    </div>

    <button type="submit" class="btn btn-primary">Сохранить</button>
</form>